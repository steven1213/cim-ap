package com.cim.spring.support;

/**
 * cim-spring-support Spring 适配层模块占位标记。
 *
 * <p>承载基类、工具、注解与 AOP 切面等公共能力，依赖 cim-core。
 * 详细实现见落地路线 P0/P4 等阶段（如统一响应、全局异常、限流幂等切面）。
 */
public final class CimSpringSupportMarker {
    private CimSpringSupportMarker() {
    }
}
